import{jsx as _jsx}from"react/jsx-runtime";import{match}from"path-to-regexp";import{Children,useEffect,useState}from"react";import{EVENTS}from"./utils/consts";import{getCurrentPath}from"./utils/navigation";export function Router({children,routes=[],defaultComponent:DefaultComponent=()=>_jsx("h1",{children:"404"})}){const[currentPath,setCurrentPath]=useState(getCurrentPath());useEffect(()=>{const onLocationChange=()=>{setCurrentPath(getCurrentPath())};window.addEventListener(EVENTS.PUSHSTATE,onLocationChange);window.addEventListener(EVENTS.POPSTATE,onLocationChange);return()=>{window.removeEventListener(EVENTS.PUSHSTATE,onLocationChange);window.removeEventListener(EVENTS.POPSTATE,onLocationChange)}},[]);const routesFromChildren=children&&Children.map(children,({props,type})=>{const{name}=type;const isRoute=name==="Route";return isRoute?props:null}).filter(Boolean);const routesToUse=routes.concat(routesFromChildren??[]);let routeParams={};const Component=routesToUse.find(({path})=>{if(path===currentPath)return true;const matcherUrl=match(path,{decode:decodeURIComponent});const matched=matcherUrl(currentPath);if(!matched)return false;routeParams=matched.params;return true})?.component??DefaultComponent;return _jsx(Component,{routeParams:routeParams})}